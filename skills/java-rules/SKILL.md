---
name: java-rules
description: 查询、解释和比较 Java 后端开发规则，说明适用条件、风险、正反例和允许的例外。Use when the user asks about a backend constraint without requesting code changes.
---

# /java-rules

根据问题关键词读取根目录对应的 `references/` 文件，先说明规则适用范围和等级，再给出正例、反例、风险和项目差异。只咨询规则时不修改代码；如果项目约定与通用规则不同，应明确指出覆盖关系。

共享规则入口：`../../SKILL.md`。
