---
name: java-developer-skills
description: 在用户明确请求 Java/Spring 后端代码、设计、Review、测试或修复时提供少量相关工程约束。优先完成用户任务，只在安全、数据、事务或接口边界确实相关时加载规则；不用于泛化的编程问答或无关语言任务。
---

# Java 后端约束

本 Skill 只提供决策支持，不替代用户需求、项目现状或正常的工程判断。先完成任务，再补充最相关的约束；不要为了“看起来规范”而增加用户未要求的层、字段、框架、事务、重试或测试。

## 工作方式

自包含任务的读取边界：如果用户没有指向当前项目、文件或变更，不要枚举工作区，也不要读取 README、评测文件、模板或无关入口；通常不读取任何参考文件，只有规则细节会影响答案时才读 1～2 个。

1. 先识别用户要交付什么，并直接回答或实现；不要先输出规约清单。
2. 只有任务明确指向当前项目、文件或变更时，才读取相关构建文件、配置和同类代码；自包含问题不要扫描工作区。
3. 只有项目约定可能改变实现时才读取 `memory.md` 或 `project/<项目名>.md`；不要把它们当作每次任务的必读文件。
4. 只读取会改变当前决策的 1～2 个 `references/` 文件；普通 CRUD、简单语法和用户已给出完整上下文时可以不读。
5. 代码修改按风险验证：小改动做针对性检查；权限、SQL、事务、并发、迁移等高风险改动才增加专项测试或回归计划。
6. 输出以用户可运行的结果为主，规则只解释与当前决策有关的部分；不要重复 Skill 内容、罗列所有未涉及风险或强行套模板。

## 路由表

| 任务/关键词 | 首选参考 |
|---|---|
| Java 基础、命名、日期、金额、集合、空值 | `references/java-core.md` |
| Controller、DTO、REST、参数校验 | `references/spring-web.md`、`references/exception-validation.md` |
| 异常、错误码、日志 | `references/exception-validation.md`、`references/contracts/exception.md`、`references/contracts/logging.md` |
| SQL、表、索引、分页、ORM | `references/database-sql.md` |
| 事务、幂等、状态流转、一致性 | `references/transaction-consistency.md` |
| Redis、缓存、分布式锁 | `references/redis-cache.md` |
| MQ、重复消费、重试、死信 | `references/message-queue.md` |
| 认证、授权、注入、脱敏、上传 | `references/security.md` |
| 线程池、超时、重试、限流、降级 | `references/concurrency-reliability.md` |
| 单测、集成测试、Testcontainers | `references/testing.md` |
| 分层、模块、依赖方向 | `references/architecture.md` |
| 响应、错误码、分页、公共字段 | `references/contracts/` 下对应文件 |

## 命令式入口

用户可以在请求开头使用以下入口别名。入口只是任务模式，不改变规则优先级；命中后仍需识别项目技术栈并按需读取参考文件。

| 入口 | 模式 | 默认读取 |
|---|---|---|
| `/java-dev` | 新功能开发、代码生成 | 仅读取当前实现实际涉及的主题 |
| `/java-design` | 接口、数据库、事务和架构设计 | 仅读取用户点名或方案实际涉及的主题 |
| `/java-review` | 代码审查、提交前自检 | 先根据证据选择主题，最多 3 个 |
| `/java-test` | 单元、集成和可靠性测试 | 测试主题 + 被测模块的一个专项主题 |
| `/java-fix` | Bug 定位和最小修复 | 异常日志 + 可疑模块规则 + 测试 |
| `/java-refactor` | 保持行为不变的渐进式重构 | 架构 + Java 核心 + 测试 |
| `/java-rules` | 查询、解释或比较规则 | 用户点名的参考文件 |

## 规则等级

- **BLOCKER**：安全漏洞、数据损坏、严重一致性或线上故障风险；必须处理或明确记录例外。
- **MUST**：生产代码默认必须遵守。
- **SHOULD**：默认遵守；有项目或性能理由时可以例外并说明。
- **MAY**：可选建议或团队偏好。

裁决方式：用户需求决定目标和范围，已有行为决定兼容约束；安全、数据正确性和明确的线上风险不能为了风格或“看起来更干净”被绕过。重构存量代码时优先保持行为不变；不要把阈值（如方法行数、批量大小）机械判定为缺陷，应结合数据量、调用频率和运行环境。`MUST` 只在其适用条件成立时生效，不要把局部建议扩展成全局禁令。

## 生成与 Review 约定

生成代码时只有在项目已有模板或用户明确要求时才读取 `templates/`。Review 时先检查用户点名的风险和需求行为，再按证据补充数据、事务、安全或性能检查；不要对每段代码机械执行完整清单。

Review 输出采用：

```text
[等级] 规则编号
位置：文件:行号（可确定时）
问题：
风险：
建议：
例外条件：
```

只对机械且低风险的问题直接修复；事务边界、接口行为、缓存一致性、数据库迁移和权限逻辑应先说明影响，再按用户授权修改。

## 适用边界

- 生产代码和 PR Review：完整启用。
- Demo、脚本、一次性迁移：保留 BLOCKER/MUST，SHOULD 可按成本豁免。
- 遗留代码：分阶段改造，不要求一次性清零全部问题。
- 用户明确要求不应用本 Skill：停止应用本 Skill 的约束。

详细规则请按路由表按需读取；项目特定约定放入 `project/`，不要修改本文件来容纳单个项目的偏好。
