id: cache-mq
title: 缓存一致性与消息重复消费
description: 验证 Skill 是否同时覆盖缓存失效、消费者幂等、重试和死信。
input:
  prompt: |
    设计订单详情缓存和订单创建消息消费者。要求说明数据库更新与缓存失效顺序、缓存穿透/击穿风险，以及消息重复投递、有限重试和死信处理方案。
constraints:
  timeout_seconds: 420
  max_turns: 8
expect:
  must_contain:
    - "缓存"
    - "幂等"
    - "重试"
    - "死信"
