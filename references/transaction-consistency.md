# 事务、一致性与幂等

- 事务边界放在应用/服务层，覆盖同一数据库内必须原子完成的写操作。
- 不要在数据库事务中执行不可控的 HTTP/RPC、长时间等待或大批量外部 IO；需要时采用事件、Outbox 或补偿方案。
- 注意 Spring 代理边界：同类自调用不会触发另一个事务方法的代理语义。
- 重试必须与幂等配套；使用业务唯一键、幂等记录或状态机防止重复扣款、重复发货等副作用。
- 明确状态迁移的合法前置状态和并发控制（乐观锁、条件更新或行锁），不要用“先查再改”假设不存在并发。
- 分布式事务不是默认选项；先评估最终一致性、消息可靠投递和补偿路径。
