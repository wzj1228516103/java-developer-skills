# Redis 与缓存

- Key 包含项目、模块、业务和标识，格式服从项目约定；所有写入设置 TTL，除非明确是持久数据。
- 设计缓存穿透、击穿、雪崩、大 Key 和热 Key 的处理；不能把缓存命中当作权限事实来源。
- 更新通常采用先提交数据库再失效缓存，并评估并发读写窗口；强一致场景使用更严格的方案。
- 分布式锁必须有唯一 key、持有者校验、过期时间和 finally 释放；不要用不可控的永久锁。
- 序列化格式、版本兼容和空值缓存时长写入项目约定。
