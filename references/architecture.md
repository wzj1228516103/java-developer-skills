# 工程结构与架构

- 依赖方向从接口/应用层指向领域和基础设施的抽象；避免 Controller 直接访问 Mapper、跨模块绕层调用。
- DTO/Command、领域对象、持久化对象、Response 按职责隔离；只有确有理由时才共享类型。
- 公共模块保持小而稳定，避免把业务逻辑塞进 utils/common；模块边界通过接口、事件或明确的应用服务沟通。
- 采用 DDD、传统分层或六边形架构时，以业务复杂度和团队能力为依据，不把架构名词当作质量证明。
