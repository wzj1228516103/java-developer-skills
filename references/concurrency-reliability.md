# 并发与可靠性

- 线程池统一管理并命名，设置队列、拒绝策略和关闭行为；禁止为每个请求裸建线程。
- 外部调用设置连接、读取和整体超时；重试采用有限次数、退避和抖动，并避免对非幂等操作盲目重试。
- CompletableFuture/异步任务显式处理异常、取消和上下文传播；不要静默丢失失败。
- 锁只保护必要临界区；评估死锁、锁续期、故障释放和降级路径。
- 限流、熔断、降级要说明触发条件、用户可见结果和恢复方式。
