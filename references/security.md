# 后端安全

## BLOCKER

- 每个需要登录的资源都校验身份和资源归属；不能信任前端传来的 userId/tenantId。
- 密码使用经过验证的密码哈希算法；密钥和 Token 不写入仓库、日志或异常消息。
- 防 SQL 注入、命令注入、SSRF、路径穿越、反序列化和不安全文件上传；采用参数绑定、白名单和大小/类型限制。

## MUST

- 权限校验覆盖新增、修改、删除、导出等副作用操作；租户边界进入查询条件或安全上下文。
- 敏感字段按最小暴露原则返回并脱敏；配置通过环境/密钥管理注入。
- 认证、授权、限流和审计日志与业务逻辑分层，避免只在前端隐藏按钮。
